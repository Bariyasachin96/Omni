# Every android.* symbol the app uses that does not exist at minSdk, with the
# guard that makes it safe. Reviewed by hand -- a compiler cannot tell a guarded
# call from an unguarded one, so this file is the record that someone looked.
# Format: <File.kt>:<Symbol>
#
# Reviewed 2026-09-10, when this check was written and found two real crashes.

EasyVoiceTtsService.kt:POST_NOTIFICATIONS            # API 33, hasNotificationPermission()
MainActivity.kt:POST_NOTIFICATIONS                   # API 33, guarded at both sites
EasyVoiceTtsService.kt:FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK  # API 29, a static final int the
                                                     # compiler inlines, handed to ServiceCompat,
                                                     # which only passes it on at 29+
EasyVoiceTtsService.kt:AudioFocusRequest             # API 26, requestAudioFocus26() -- and the
                                                     # field's TYPE, which is resolved lazily and
                                                     # never touched on the pre-26 path
EasyVoiceTtsService.kt:abandonAudioFocusRequest      # API 26, abandonAudioFocus26()
Locales.kt:of                                        # API 36, Locale.of, only at SDK_INT >= 36
TroubleshootActivity.kt:isBackgroundRestricted  # API 28, only at SDK_INT >= 28 in systemFindings()
